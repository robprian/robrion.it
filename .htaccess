Options -Indexes
RewriteEngine On

# Biarkan file & folder nyata diakses langsung (assets, api, dll)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Semua route lain → index.php
RewriteRule ^ index.php [QSA,L]
